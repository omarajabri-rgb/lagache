<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />

  <title>LA GÂCHE | Gourmet Food Services</title>
  <meta name="description" content="LA GÂCHE offers refined gourmet food services in Amman, Jordan. Crafted menus, elegant presentation, and professional execution." />
  <link rel="canonical" href="https://www.la-gache.com/" />

  <style>
    :root{
      --black:#0f0f0f;
      --gold:#c9a46b;
      --muted:#6b7280;
      --bg:#ffffff;
      --max:1100px;
    }

    *{box-sizing:border-box}
    body{
      margin:0;
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      background:var(--bg);
      color:#111;
      line-height:1.6;
    }

    a{text-decoration:none; color:inherit}

    header{
      border-bottom:1px solid #eee;
      background:#fff;
    }

    .container{
      width:min(var(--max), calc(100% - 40px));
      margin:auto;
    }

    .nav{
      display:flex;
      align-items:center;
      justify-content:space-between;
      padding:18px 0;
    }

    .brand{
      display:flex;
      align-items:center;
      gap:12px;
    }

    .brand img{
      height:46px;
    }

    .brand span{
      font-size:13px;
      color:var(--muted);
      letter-spacing:1px;
    }

    .hero{
      padding:80px 0;
      text-align:center;
    }

    .badge{
      display:inline-block;
      padding:8px 14px;
      border:1px solid rgba(201,164,107,.5);
      border-radius:999px;
      font-weight:700;
      font-size:13px;
      color:#7a5b2b;
      background:rgba(201,164,107,.15);
      margin-bottom:20px;
    }

    h1{
      font-size:clamp(34px, 5vw, 52px);
      letter-spacing:-.5px;
      margin:0 0 14px;
    }

    .lead{
      color:var(--muted);
      max-width:700px;
      margin:0 auto 28px;
      font-size:18px;
    }

    .cta{
      display:flex;
      justify-content:center;
      gap:14px;
      flex-wrap:wrap;
    }

    .btn{
      padding:14px 20px;
      border-radius:14px;
      font-weight:700;
      border:1px solid #e5e7eb;
      background:#fff;
    }

    .btn.primary{
      background:linear-gradient(90deg, var(--gold), #e3c890);
      border:none;
      color:#000;
    }

    .btn.dark{
      background:var(--black);
      color:#fff;
      border:none;
    }

    footer{
      border-top:1px solid #eee;
      padding:26px 0;
      text-align:center;
      color:var(--muted);
      font-size:14px;
    }
  </style>
</head>

<body>

<header>
  <div class="container">
    <div class="nav">
      <div class="brand">
        <img src="assets/img/la-gache-logo.png" alt="LA GÂCHE Logo">
        <span>GOURMET FOOD SERVICES</span>
      </div>
    </div>
  </div>
</header>

<main>
  <section class="hero">
    <div class="container">
      <div class="badge">Premium • Elegant • Refined</div>
      <h1>LA GÂCHE</h1>
      <p class="lead">
        Gourmet food services crafted with precision, elegance, and attention to detail.
        A refined culinary experience designed for discerning clients.
      </p>

      <div class="cta">
        <a href="#" class="btn primary">Explore Services</a>
        <a href="#" class="btn dark">Contact</a>
      </div>
    </div>
  </section>
</main>

<footer>
  © <span id="year"></span> LA GÂCHE — Amman, Jordan
</footer>

<script>
  document.getElementById("year").textContent = new Date().getFullYear();
</script>

</body>
</html>
